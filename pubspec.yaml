name: screen_adaptive_plus
description: A new Flutter package project.
version: 1.0.0
environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
